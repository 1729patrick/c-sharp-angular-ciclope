@model Ciclope.Models.DocRelatorioUnico

@{
    ViewData["Title"] = "Relatório Único";
}

<style>
    .containerPDF {
        width: 100%;
        height: 500px;
        margin: 0 auto;
        margin-bottom: 1rem !important;
    }
</style>

<div class="row h-100">
    <partial name="_DisplaySideMenuPartial" for="IdEmpresa" />
    <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-4">
        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
            <h1 class="h2">@ViewData["Title"]</h1>
            <div class="btn-toolbar mb-2 mb-md-0">
                <div class="btn-group mr-2">
                    <!-- <a href="@Url.Action("DownloadAFile", "DocRelatorioUnico", new { id = Model.Id})" class="btn btn-sm btn-outline-secondary">
                        Anexo A
                    </a>
                    <a href="@Url.Action("DownloadBFile", "DocRelatorioUnico", new { id = Model.Id})" class="btn btn-sm btn-outline-secondary">
                        Anexo B
                    </a>
                    <a href="@Url.Action("DownloadCFile", "DocRelatorioUnico", new { id = Model.Id})" class="btn btn-sm btn-outline-secondary">
                        Anexo C
                    </a>
                    <a href="@Url.Action("DownloadDFile", "DocRelatorioUnico", new { id = Model.Id})" class="btn btn-sm btn-outline-secondary">
                        Anexo D
                    </a>
                    <a href="@Url.Action("DownloadEFile", "DocRelatorioUnico", new { id = Model.Id})" class="btn btn-sm btn-outline-secondary">
                        Anexo E
                    </a>
                    <a href="@Url.Action("DownloadFFile", "DocRelatorioUnico", new { id = Model.Id})" class="btn btn-sm btn-outline-secondary">
                        Anexo F
                    </a>
                    <a href="@Url.Action("DownloadFile", "DocRelatorioUnico", new { id = Model.Id})" class="btn btn-sm btn-outline-secondary">
                        Anexo 0
                    </a> -->
                    <a href="@Url.Action("DownloadCertFile", "DocRelatorioUnico", new {id = Model.Id})" class="btn btn-sm btn-outline-secondary">
                        Certificado
                    </a>
                </div>
            </div>
        </div>
        <div class="d-md-flex">
            <div class="col-md-6">
                <p style="margin-left: 20px;">Data: @Model.Ano</p>
            </div>
            <div class="col-md-6">
                <div class="containerPDF">
                    <embed src="@Url.Action("GetFile", "DocRelatorioUnico", new { id = Model.Id})" style="width:100%; height:500px; border-radius:5px; border:1px solid #bbb9b9">
                </div>
            </div>
        </div>
    </main>
</div>
